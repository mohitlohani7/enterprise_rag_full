def classify_query(query: str):
    """
    Basic classifier — always return general.
    Later you can add:
      - arithmetic
      - reasoning
      - search
      - summarization
      - legal, medical
    """
    return "general"
